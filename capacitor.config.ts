import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'jose_munnoz_prueba2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
